# WalletConnect Project ID
# Get yours at https://cloud.walletconnect.com
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_project_id_here

# Deployer Private Key (for contract deployment)
# IMPORTANT: Never commit this! Use a dedicated deployment wallet.
PRIVATE_KEY=your_private_key_here

# Score Signer Private Key (for backend server)
# Generate with: cd backend && npm run generate-signer
# IMPORTANT: Keep this SECRET - it validates all scores!
SCORE_SIGNER_PRIVATE_KEY=your_score_signer_private_key_here

# Backend API URL (for score signing)
NEXT_PUBLIC_API_URL=http://localhost:3001
